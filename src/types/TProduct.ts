type TProduct = {
  title: string;
  price: number;
  // createdAt: Date;
  mainCategory: string;
  subCategory: string;
  isSale: boolean;
  isNew: boolean;
  detailImage: string;
  productImages: string[];
};

export default TProduct;
